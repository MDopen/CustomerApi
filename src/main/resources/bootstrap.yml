spring:
  application:
    name: api-customer
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888/
      label: local
management:
  endpoints:
    web:
      exposure:
        include: "*"
